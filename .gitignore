# Dependencies
node_modules/

# ESLint cache
.eslintcache

# VS Code settings
.vscode/

# OS files
.DS_Store
Thumbs.db

# Environment files (API keys — keep these private!)
.env
.env.local

# config.js is committed so the app works on GitHub Pages.
# (Free-tier API keys — acceptable for a class project deployment.)